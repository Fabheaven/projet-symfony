
{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Inscription</h1>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}
    
    {% for message in app.flashes('error') %}
        <div class="alert alert-danger">{{ message }}</div>
    {% endfor %}

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email, {'label': 'E-mail'}) }}
        {{ form_row(registrationForm.pseudo, {'label': 'Pseudo'}) }}
        {{ form_row(registrationForm.password, {'label': 'Mot de passe'}) }}
        {{ form_row(registrationForm.confirmPassword, {'label': 'Confirmer le mot de passe'}) }}
        {{ form_row(registrationForm.submit) }}
        {# Lien vers la page d'inscription #}
  <p>Si déja inscrit ! <a href="{{ path('app_login') }}">Se connecter</a></p>
    {{ form_end(registrationForm) }}
{% endblock %}
